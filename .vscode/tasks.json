{
   // See https://go.microsoft.com/fwlink/?LinkId=733558
   // for the documentation about the tasks.json format
   "version": "2.0.0",
   "tasks": [
      {
         "label": "zip project (bash)",
         "type": "shell",
         "command": "mkdir -p build && rm -f build/basic-tests-plugin.zip && zip -r build/basic-tests-plugin.zip manifest.json mcp-echo-tools.json color.png outline.png skills/",
         "problemMatcher": []
      }
   ]
}